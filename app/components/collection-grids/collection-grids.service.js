(function(angular) {
    'use strict';

    angular.module('omtApp').factory('collectionGridsService', ['storeService', 'TILE_SET_ITEMS', function (storeService, TILE_SET_ITEMS) {

        var service = {

        };

        return service;
    }]);

})(window.angular);
