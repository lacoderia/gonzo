'use strict';

// Declare app level module which depends on views, and components
angular.module('omtApp', [
    'ngRoute',
    'ngResource',
    'ngMaterial',
    'LocalStorageModule',
    'customizer'
]);